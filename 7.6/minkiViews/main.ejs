<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script>
        //Javascript를 사용하는 공간. 여기에 주소를 이동하는 함수를 생성 할 것이다
        function add(){
            location.href = "/board/add" // localhost:3000/board/add 주소로 이동한다는 뜻
        }    
    </script>
</head>
<body>
    <h1>회원전용 메인페이지 입니다</h1>
    <table border="1">
        <tr>
            <th>글 번호</th>
            <th>글 제목</th>
            <th>작성자</th>
        </tr>
        <% for(let i = 0; i < content.length; i++){ %>
        <tr onclick="contents('<%=content[i].No%>')">
                <td><%=content[i].No%></td>
                <td><%=content[i].title%></td>
                <td><%=content[i].writer%></td>
        </tr>  
        <% } %> 
        </table>

    <!-- 게시글 작성 버튼 -->
    <button onclick="add()">글 작성하기</button>

    <script>
        // 클릭한 게시글의 정보를 담아서 info 주소로 보내주기
        function contents(no){
            location.href = "/board/info?_no="+no; //location.href는 GET방식의 호출이다!! = url에 실어서 보낸다
                                                 //locatlhost:3000?key1=value1&key2=value2 의 형태로 맞춰주면된다
                                                 //이때 ?값뒤에 보내줄 데이터의 key와 value값을 정해준다
        }
    </script>
</body>
</html>